1、 jwt获取，jwt是无状态的，每次登陆都拿到token   redis塞token  下次来的时候   从redis判断这个token与redis里是否相同  不相等就返回未登录，重新登陆
2、可以判断在线人数：每次登陆都获取token，比如30分钟无操作token就失效，这个token是无限期的，每次发出请求都增加redis存活时间，


另外一种方式，动态方式，前端每次获取 当前时间+要加密的值+key：
前端：当前时间、name、key

key：md5（前后端约定好的加盐值）

后台:获取到前端的值然后进行md5加密，判断是否相同